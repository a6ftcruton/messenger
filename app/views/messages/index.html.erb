<h2><%= "Welcome #{current_user.username}"  %></h2>
<section class="messages">
  <ul>
  <% @messages.each do |message| %>
    <li class="message"><%= link_to message.subject, message_path(message) %></li>
  <% end %>
</ul>
</section>
